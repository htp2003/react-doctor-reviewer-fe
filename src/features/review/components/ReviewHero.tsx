export function ReviewHero() {
  return (
    <section className="hero-banner">
      <div>
        <p className="eyebrow">React Doctor Reviewer</p>
        <h1>Ship a frontend review tool that stays tidy as it grows.</h1>
      </div>
      <p className="hero-copy">
        The UI is split by feature, the API lives behind a small typed gateway, and the
        result panels are ready for new filters, tabs, or charts later.
      </p>
    </section>
  )
}
